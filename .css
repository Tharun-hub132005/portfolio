/* Base styles for larger screens */
.nav-links {
    display: flex;
}
.burger {
    display: none; /* Hide burger icon on desktop */
}

/* Styles for screens smaller than 768px (e.g., tablets & mobiles) */
@media (max-width: 768px) {
    .nav-links {
        /* Hide by default on mobile, show with JS */
        display: none;
        flex-direction: column;
        width: 100%;
        position: absolute;
        top: 60px; /* Below header */
        left: 0;
        background-color: #333;
    }
    .nav-links.active { /* Class added by JS when burger clicked */
        display: flex;
    }
    .burger {
        display: block; /* Show burger icon on mobile */
        cursor: pointer;
    }
    /* Adjust grid layouts to stack vertically */
    .skills-grid, .projects-grid {
        grid-template-columns: 1fr; /* Single column on mobile */
    }
    .about-content {
        flex-direction: column;
        text-align: center;
    }
    .about-content img {
        margin-bottom: 20px;
    }
}
